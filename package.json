{
  "name": "crm-project-root",
  "version": "1.0.0",
  "description": "Root package.json for Render deployment",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "cd server && npm start"
  }
}
